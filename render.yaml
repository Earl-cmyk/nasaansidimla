services:
  - type: web
    name: nasaansidimla
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT backend.app:app
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false